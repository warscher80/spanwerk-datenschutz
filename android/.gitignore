gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Ausnahme: der bewusst mitgelieferte, feste Release-Keystore (nur gleichbleibende
# Signatur, kein Geheimnis) muss ins Repo, damit jedes Build gleich signiert.
!app/kickprophet-release.jks
